#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
source "${SCRIPT_DIR}/../_lib/tap_helpers.sh"

echo "1..1"
echo "# repo-local-tooling family for {{ slots.project_name }}"
tap_ok 1 "family marker"
