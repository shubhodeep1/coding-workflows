{
  "category": "patterns",
  "confidence": 0.7,
  "details": "=== CONSENSUS FINDINGS ===\n- scripts/review_collect_pr_metadata.sh:78-90 | severity=medium | confidence=5\n  flagged_by: [deepseek_deepseek-v4-pro, minimax_minimax-m2_5, moonshotai_kimi-k2_5, qwen_qwen3_6-plus, x-ai_grok-4_20]\n  PROBLEM: `count` from `jq 'length'` is used in integer tests and the loop bound without numeric validation, so malformed or non-array JSON can crash the fallback helper.\n  WHY: Under `set -euo pipefail`, a non-numeric `count` triggers an integer-expression failure and aborts instead of returning the documented fail-open `[]` result.\n- scripts/review_collect_pr_metadata.sh:120-146 | severity=medium | confidence=4\n  flagged_by: [deepseek_deepseek-v4-pro, x-ai_grok-4_20]\n  PROBLEM: GraphQL responses that contain `.errors` but no populated `data.repository` are transformed into `[]` and returned with exit 0, so fallback hydration can fail silently.\n  WHY: Because the caller only warns when the helper returns non-zero, these GraphQL-level failures emit no operator-visible signal and lose the fallback context entirely.\n- scripts/review_collect_pr_metadata.sh:93-104 | severity=low | confidence=3\n  flagged_by: [moonshotai_kimi-k2_5, qwen_qwen3_6-plus, x-ai_grok-4_20]\n  PROBLEM: The helper parses `numbers_json` with `jq` once per element plus a separate length check, causing up to 21 `jq` subprocesses per call.\n  WHY: This adds avoidable fork overhead in the batched fallback path, even though it does not change correctness.\n- scripts/review_collect_pr_metadata.sh:70-89 | severity=low | confidence=3\n  flagged_by: [qwen_qwen3_6-plus]\n  PROBLEM: The GraphQL helper has no internal guard on how many issue aliases it will generate, so a future caller or raised cap could build an oversized query.\n  WHY: The current 20-issue cap masks the problem today, but without an internal bound GitHub could reject a larger request if the function is reused elsewhere.\n=== END CONSENSUS FINDINGS ===\n\n=== CONSENSUS TASK GAPS ===\n(No task gaps reported.)\n=== END CONSENSUS TASK GAPS ===\n\n=== FINDINGS FROM deepseek_deepseek-v4-pro ===\n- File: scripts/review_collect_pr_metadata.sh\n  Line or code reference: Lines 85-86 in function _fetch_linked_issue_bodies_graphql\n  Problem: The variable `count` is populated from `jq 'length'` and used directly in `[ \"${count}\" -eq 0 ]` without validating that `count` is numeric.\n  Why it fails at runtime: If `jq` yields `null` or another non-numeric value, bash aborts with \"integer expression expected\" under `set -euo pipefail` instead of returning the documented fail-open `[]` result.\n  ISSUE_CONFIDENCE: 3\n- File: scripts/review_collect_pr_metadata.sh\n  Line or code reference: Lines 129-146 in function _fetch_linked_issue_bodies_graphql (the jq transform block)\n  Problem: GraphQL responses that contain an `errors` array but no populated `data.repository` are transformed into `[]` with exit code 0.\n  Why it fails at runtime: The caller skips the warning path when `_fallback_fetch_status` stays 0, so GraphQL-level failures produce no operator-visible signal and no fallback hydration context.\n  ISSUE_CONFIDENCE: 2\n=== END FINDINGS FROM deepseek_deepseek-v4-pro ===\n\n=== FINDINGS FROM minimax_minimax-m2_5 ===\n- File: scripts/review_collect_pr_metadata.sh\n  Line: 78-87\n  Problem: The `count` variable is populated from `jq 'length'` and used in integer comparison `[ \"${count}\" -eq 0 ]` without validating it is numeric.\n  Why it fails at runtime: When non-array JSON is passed to the function, the script aborts instead of returning the documented fail-open `[]` result.\n  ISSUE_CONFIDENCE: 5\n=== END FINDINGS FROM minimax_minimax-m2_5 ===\n\n=== FINDINGS FROM mistralai_mistral-small-2603 ===\n(No findings reported.)\n=== END FINDINGS FROM mistralai_mistral-small-2603 ===\n\n=== FINDINGS FROM moonshotai_kimi-k2_5 ===\n- File: scripts/review_collect_pr_metadata.sh\n  Line or code reference: Lines 85-86\n  Problem: The `count` variable populated from `jq 'length'` is used directly in arithmetic comparison `[ \"${count}\" -eq 0 ]` without numeric validation.\n  Why it fails at runtime: The script aborts instead of following the documented fail-open path that returns `[]`, breaking the fallback linked-issue hydration.\n  ISSUE_CONFIDENCE: 4\n- File: scripts/review_collect_pr_metadata.sh\n  Line or code reference: Lines 94-95\n  Problem: Inside the loop building GraphQL fragments, `jq -r \".[$i] // empty\"` is invoked once per iteration, plus the initial length call, for a total of up to 21 `jq` invocations per function call.\n  Why it fails at runtime: This does not fail functionally, but it adds unnecessary overhead that could be avoided with a single `jq` pass.\n  ISSUE_CONFIDENCE: 2\n=== END FINDINGS FROM moonshotai_kimi-k2_5 ===\n\n=== FINDINGS FROM qwen_qwen3_6-plus ===\n- File: scripts/review_collect_pr_metadata.sh\n  Line or code reference: lines 85\u201387\n  Problem: `jq 'length'` may yield non-numeric output for some inputs, and `[ \"${count}\" -eq 0 ]` then crashes with \"integer expression expected\" under `set -e`.\n  Why it fails at runtime: The function should fail open with `[]`, but a bad `count` aborts fallback hydration instead.\n  ISSUE_CONFIDENCE: 4\n- File: scripts/review_collect_pr_metadata.sh\n  Line or code reference: lines 70\u201389\n  Problem: The `_fetch_linked_issue_bodies_graphql` function builds one `issueOrPullRequest(number: N)` alias per issue number without an internal size guard.\n  Why it fails at runtime: Today the caller\u2019s 20-issue cap prevents overflow, but a future caller or higher cap could produce a query that GitHub rejects for size or alias-count reasons.\n  ISSUE_CONFIDENCE: 3\n- File: scripts/review_collect_pr_metadata.sh\n  Line or code reference: lines 93\u201396\n  Problem: The loop invokes `jq` once per issue number, plus the initial `jq 'length'` call, so the same JSON array is parsed N+1 times.\n  Why it fails at runtime: It works, but it creates unnecessary process-spawn overhead that a single `jq` extraction could avoid.\n  ISSUE_CONFIDENCE: 3\n=== END FINDINGS FROM qwen_qwen3_6-plus ===\n\n=== FINDINGS FROM x-ai_grok-4_20 ===\n- File: scripts/review_collect_pr_metadata.sh\n  Line or code reference: 78-90 (_fetch_linked_issue_bodies_graphql function, count handling + loop)\n  Problem: `count=\"$(... | jq 'length' 2>/dev/null || echo 0)\"` followed by `if [ -z \"${count}\" ] || [ \"${count}\" -eq 0 ]; then` and a for-loop using `$count` directly has no validation that `count` is a valid integer.\n  Why it fails at runtime: If `jq` produces non-numeric output, the arithmetic test or `(( ))` loop aborts the script early instead of following the documented fail-open path that returns `[]`.\n  ISSUE_CONFIDENCE: 5\n- File: scripts/review_collect_pr_metadata.sh\n  Line or code reference: 120-140 (the jq transform after gh_retry graphql)\n  Problem: The transform assumes a well-formed `.data.repository` and can drop the entire fallback path when GraphQL returns errors or a partial response.\n  Why it fails at runtime: A transient GraphQL failure can now lose all fallback context instead of preserving any partial success, so the batched path is less resilient than the old per-issue REST loop.\n  ISSUE_CONFIDENCE: 4\n- File: scripts/review_collect_pr_metadata.sh\n  Line or code reference: 94-104 (the for-loop that builds the fragment)\n  Problem: For each number the loop runs a separate `jq -r \".[$i] // empty\"` invocation, plus the initial length call.\n  Why it fails at runtime: This is functionally correct but adds avoidable subprocess overhead in a hot CI path.\n  ISSUE_CONFIDENCE: 3\n=== END FINDINGS FROM x-ai_grok-4_20 ===",
  "fingerprint": "1f2a2c2d24c88993b257351b6b834da167940867dc2f28e8d299313cacfc009e",
  "lineage": {
    "issue_number": null,
    "parent_ids": [],
    "pr_number": 3333,
    "run_id": "27468794477",
    "superseded_by": null,
    "supersedes": null
  },
  "provenance": {
    "actor": "shubhodeep1",
    "run_attempt": 1,
    "run_id": "27468794477",
    "source_refs": [
      "reviewer_consensus.txt"
    ],
    "workflow": "review_autofix"
  },
  "record_id": "mem_20260613144045_d24bd8cc9a",
  "schema_version": "memory_record.v1",
  "scope": {
    "issue_number": null,
    "level": "run",
    "pr_number": 3333,
    "run_id": "27468794477"
  },
  "sensitive": false,
  "status": "candidate",
  "summary": "Reviewer consensus flagged 3 multi-reviewer item(s) across 4 total consensus item(s)",
  "timestamps": {
    "created_at": "2026-06-13T14:40:45Z",
    "promoted_at": null,
    "superseded_at": null
  }
}
