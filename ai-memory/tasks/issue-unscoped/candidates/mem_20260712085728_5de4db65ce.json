{
  "category": "patterns",
  "confidence": 0.7,
  "details": "=== CONSENSUS FINDINGS ===\n- scripts/review_run_reviewers.sh:2268 | severity=high | confidence=5\n  flagged_by: [deepseek_deepseek-v4-pro, minimax_minimax-m2_5, mistralai_mistral-small-2603, moonshotai_kimi-k2_5, qwen_qwen3_6-plus, x-ai_grok-4_20]\n  PROBLEM: The reviewer body render invocation uses `${SUPPORT_SCRIPTS_DIR}/render_prompt.sh` without the `${SUPPORT_SCRIPTS_DIR:-scripts}` fallback pattern used elsewhere in the file.\n  WHY: With `set -u` active and `SUPPORT_SCRIPTS_DIR` unset, the script hard-fails before render; with an empty variable it resolves to `/render_prompt.sh` and fails \"No such file or directory\".\n- tests/test_assemble_prompt.py:285-316 | severity=high | confidence=5\n  flagged_by: [deepseek_deepseek-v4-pro, minimax_minimax-m2_5, mistralai_mistral-small-2603, moonshotai_kimi-k2_5, qwen_qwen3_6-plus, x-ai_grok-4_20]\n  PROBLEM: The regression tests copy the ambient environment via `_base_env()` and do not clear `RENDER_PROMPT_INPUT_ALREADY_ASSEMBLED`, so the first new test can take the assembled-input path and return 0 instead of the asserted 1.\n  WHY: Ambient environment pollution makes the test flaky, and the companion flag-gated test should also clear the variable before setting its intended value.\n- scripts/render_prompt.sh:253-278 | severity=low | confidence=4\n  flagged_by: [mistralai_mistral-small-2603, x-ai_grok-4_20]\n  PROBLEM: New shell identifier `INPUT_ALREADY_ASSEMBLED_FLAG_ADDED` and the public env var `RENDER_PROMPT_INPUT_ALREADY_ASSEMBLED` were added without updating the identifier-uniqueness contract surface.\n  WHY: The PR claim that new identifiers were verified collision-free is unsupported by evidence, so future collisions or renames remain possible.\n=== END CONSENSUS FINDINGS ===\n\n=== CONSENSUS TASK GAPS ===\n- requirement: \"This should also be ported to `main`, or the next `main \u2192 stable` promotion will silently revert it.\"\n  expected_change_site: `.github/workflows/promote-main-to-stable.yml`\n  confidence=3\n  flagged_by: [deepseek_deepseek-v4-pro]\n  EVIDENCE: No branch-target metadata, workflow change, or PR-body instruction in the diff ensures this fix lands on `main` before the next stable promotion.\n=== END CONSENSUS TASK GAPS ===\n\n=== FINDINGS FROM deepseek_deepseek-v4-pro ===\n- scripts/review_run_reviewers.sh:2268 | severity=high | confidence=5\n  PROBLEM: The reviewer body render invocation uses `${SUPPORT_SCRIPTS_DIR}/render_prompt.sh` without the `${SUPPORT_SCRIPTS_DIR:-scripts}` fallback that every other reference to `${SUPPORT_SCRIPTS_DIR}` in the same file uses.\n  WHY: With `set -u` active and `SUPPORT_SCRIPTS_DIR` unset, the script hard-fails before the render runs; with an empty variable it resolves to `/render_prompt.sh` and fails with \"No such file or directory\".\n- tests/test_assemble_prompt.py:285-287 | severity=high | confidence=5\n  PROBLEM: The first new test uses `_base_env()` and never clears `RENDER_PROMPT_INPUT_ALREADY_ASSEMBLED`, so ambient environment can make the test take the assembled-input path and return 0 instead of the asserted 1.\n  WHY: The test's invariant is violated by environment pollution, making it flaky on machines or CI where the variable is pre-set.\n- Requirement: \"This should also be ported to `main`, or the next `main \u2192 stable` promotion will silently revert it.\"\n  Expected change site: `.github/workflows/promote-main-to-stable.yml`\n  Evidence of absence: No branch-target metadata, workflow change, or PR-body instruction in the diff ensures this fix lands on `main` before the next stable promotion.\n  ISSUE_CONFIDENCE: 3\n=== END FINDINGS FROM deepseek_deepseek-v4-pro ===\n\n=== FINDINGS FROM minimax_minimax-m2_5 ===\n- scripts/review_run_reviewers.sh:2268 | severity=high | confidence=5\n  PROBLEM: The reviewer body render invocation uses `\"${SUPPORT_SCRIPTS_DIR}/render_prompt.sh\"` without the `:-scripts` fallback that is consistently used elsewhere in the same file.\n  WHY: If `SUPPORT_SCRIPTS_DIR` is unset or empty, the shell expands it to `/render_prompt.sh`, which is an invalid path and causes the script to fail with \"No such file or directory\".\n=== END FINDINGS FROM minimax_minimax-m2_5 ===\n\n=== FINDINGS FROM mistralai_mistral-small-2603 ===\n- scripts/review_run_reviewers.sh:2268 | severity=high | confidence=5\n  PROBLEM: The reviewer base body render invocation uses `${SUPPORT_SCRIPTS_DIR}/render_prompt.sh` without the `${SUPPORT_SCRIPTS_DIR:-scripts}` fallback pattern consistently used in the file.\n  WHY: Under `set -euo pipefail`, an unset `SUPPORT_SCRIPTS_DIR` causes an unbound-variable error, and even without that flag it expands to `/render_prompt.sh`, which fails with \"No such file or directory\".\n- scripts/render_prompt.sh:253-278 | severity=low | confidence=4\n  PROBLEM: The new `INPUT_ALREADY_ASSEMBLED_FLAG_ADDED` boolean flag and its environment-variable-backed behavior were added without updating the repository's naming-immutability registers or uniqueness checks.\n  WHY: The explicit new-identifier collision check was not evidenced against the full surface, so the PR's collision-free claim is unsupported.\n- tests/test_assemble_prompt.py:285-287 | severity=high | confidence=5\n  PROBLEM: The first new test inherits the ambient environment via `_base_env()`, which copies `os.environ` fully, so a pre-set `RENDER_PROMPT_INPUT_ALREADY_ASSEMBLED` can make it return 0 instead of 1.\n  WHY: The test becomes flaky when environment pollution takes the assembled-input path and skips the failure the assertion expects.\n=== END FINDINGS FROM mistralai_mistral-small-2603 ===\n\n=== FINDINGS FROM moonshotai_kimi-k2_5 ===\n- scripts/review_run_reviewers.sh:2268 | severity=high | confidence=5\n  PROBLEM: Uses `\"${SUPPORT_SCRIPTS_DIR}/render_prompt.sh\"` without the `:-scripts` fallback used elsewhere in the same file.\n  WHY: If `SUPPORT_SCRIPTS_DIR` is empty or unset, bash expands this to `/render_prompt.sh`, which fails with \"No such file or directory\" under `set -e`.\n- tests/test_assemble_prompt.py:285-286 | severity=high | confidence=5\n  PROBLEM: test uses _base_env() which copies os.environ directly; if `RENDER_PROMPT_INPUT_ALREADY_ASSEMBLED` is set in the test runner environment, the test will fail because it expects returncode 1 but gets 0.\n  WHY: Test isolation failure - environment pollution causes false-positive pass on unfixed behavior.\n=== END FINDINGS FROM moonshotai_kimi-k2_5 ===\n\n=== FINDINGS FROM qwen_qwen3_6-plus ===\n- scripts/review_run_reviewers.sh:2268 | severity=high | confidence=5\n  PROBLEM: Uses `${SUPPORT_SCRIPTS_DIR}` without the `:-scripts` fallback. Every other reference in this file uses `${SUPPORT_SCRIPTS_DIR:-scripts}`.\n  WHY: If `SUPPORT_SCRIPTS_DIR` is unset or empty, bash either exits with \"unbound variable\" (`-u`) or invokes `bash \"/render_prompt.sh\"` which fails with \"No such file or directory\".\n- tests/test_assemble_prompt.py:285-297 | severity=high | confidence=5\n  PROBLEM: `_base_env()` copies `os.environ` (line 23). If `RENDER_PROMPT_INPUT_ALREADY_ASSEMBLED` is set to a truthy value in the test runner's environment, `render_prompt.sh` will take the `--input-already-assembled` path and exit 0, causing `assert proc.returncode == 1` to fail.\n  WHY: The test's invariant is violated by environment pollution, and the second new test also inherits environment pollution even though it would mask a false failure rather than a false pass.\n=== END FINDINGS FROM qwen_qwen3_6-plus ===\n\n=== FINDINGS FROM x-ai_grok-4_20 ===\n- scripts/review_run_reviewers.sh:2268 | severity=high | confidence=5\n  PROBLEM: The base reviewer prompt body render uses `bash \"${SUPPORT_SCRIPTS_DIR}/render_prompt.sh\"` with no `:-scripts` fallback while the immediately-below model-family overlay and the editor render in `review_apply_fixes.sh` both correctly use the fallback pattern.\n  WHY: With `set -euo pipefail`, an unset `SUPPORT_SCRIPTS_DIR` expands to `bash \"/render_prompt.sh\"` and terminates the entire reviewer panel.\n- tests/test_assemble_prompt.py:285-316 | severity=high | confidence=5\n  PROBLEM: Both tests do `env = _base_env()` (`os.environ.copy()`), and the first test sets only `RENDER_PROMPT_SKIP_SYNTAX_VALIDATION=1` while the second explicitly sets `RENDER_PROMPT_INPUT_ALREADY_ASSEMBLED=1`.\n  WHY: If the test runner or CI environment already exports `RENDER_PROMPT_INPUT_ALREADY_ASSEMBLED=1`, the \"hard-fails-without-flag\" test silently takes the already-assembled path and the `assert proc.returncode == 1` assertion fails.\n- scripts/render_prompt.sh:253-278 | severity=low | confidence=4\n  PROBLEM: New shell identifier `INPUT_ALREADY_ASSEMBLED_FLAG_ADDED` and the public env var `RENDER_PROMPT_INPUT_ALREADY_ASSEMBLED` were added without updating the identifier-uniqueness contract surface.\n  WHY: The registry/scan was not performed against the full surface, so future collisions or renames remain possible.\n=== END FINDINGS FROM x-ai_grok-4_20 ===",
  "fingerprint": "a2ba01e01b9df85eaa549bca8966e0e010b36fb802ed0010cf2f0ace1eb3ebf2",
  "lineage": {
    "issue_number": null,
    "parent_ids": [],
    "pr_number": 3646,
    "run_id": "29185306811",
    "superseded_by": null,
    "supersedes": null
  },
  "provenance": {
    "actor": "shubhodeep1",
    "run_attempt": 1,
    "run_id": "29185306811",
    "source_refs": [
      "reviewer_consensus.txt"
    ],
    "workflow": "review_autofix"
  },
  "record_id": "mem_20260712085728_5de4db65ce",
  "schema_version": "memory_record.v1",
  "scope": {
    "issue_number": null,
    "level": "run",
    "pr_number": 3646,
    "run_id": "29185306811"
  },
  "sensitive": false,
  "status": "candidate",
  "summary": "Reviewer consensus flagged 3 multi-reviewer item(s) across 4 total consensus item(s)",
  "timestamps": {
    "created_at": "2026-07-12T08:57:28Z",
    "promoted_at": null,
    "superseded_at": null
  }
}
