{
  "category": "patterns",
  "confidence": 0.7,
  "details": "=== CONSENSUS FINDINGS ===\n- tests/test_review_autofix_review_pipeline_contract.py:1762,2944-2964 | severity=medium | confidence=5\n  flagged_by: [deepseek_deepseek-v4-pro, minimax_minimax-m2_5, mistralai_mistral-small-2603, moonshotai_kimi-k2_5, qwen_qwen3_6-plus]\n  PROBLEM: `test_review_collect_pr_metadata_helper_caps_fallback_graphql_batch_at_twenty_issues` is defined but never invoked from `main()`.\n  WHY: The test runner only executes functions listed in `main()`, so the 20-issue cap behavior has no coverage and regressions would slip through.\n- scripts/review_collect_pr_metadata.sh:89-92 | severity=low | confidence=4\n  flagged_by: [mistralai_mistral-small-2603]\n  PROBLEM: The numeric validation path still uses shell arithmetic comparison `[ \"${count}\" -eq 0 ]`, which can error if `count` is non-integer.\n  WHY: A malformed `jq` result could fail noisily instead of gracefully despite the regex guard.\n- scripts/review_collect_pr_metadata.sh:130-134 | severity=low | confidence=3\n  flagged_by: [mistralai_mistral-small-2603]\n  PROBLEM: The GraphQL response handling does not detect cases where requested aliases resolve to null/empty values.\n  WHY: Fewer hydrated items can come back with no warning, making missing fallback context harder to spot than the previous per-issue REST warnings.\n- scripts/review_collect_pr_metadata.sh:143-157 | severity=low | confidence=4\n  flagged_by: [deepseek_deepseek-v4-pro, moonshotai_kimi-k2_5]\n  PROBLEM: The jq transform silently discards aliases that resolve to null via `select(type == \"object\" and (.number? != null))`.\n  WHY: The fail-open path keeps going but loses the per-issue warning that the previous REST loop provided, making gaps in linked-issue context harder to diagnose.\n- scripts/review_collect_pr_metadata.sh:281-295 | severity=low | confidence=4\n  flagged_by: [mistralai_mistral-small-2603, x-ai_grok-4_20]\n  PROBLEM: The caller treats mixed-success GraphQL fallback responses as success without warning.\n  WHY: Partial fallback data can be silently incomplete even though the old per-REST path warned on each failed fetch, weakening observability.\n- scripts/review_collect_pr_metadata.sh:89-92,130-134 | severity=medium | confidence=3\n  flagged_by: [x-ai_grok-4_20]\n  PROBLEM: The transform returns `number: (.number // 0)` for PullRequests, which can surface `0` where consumers expect a positive numeric ID.\n  WHY: Downstream consumers may treat `0` as invalid and mis-handle linked-issue context rendering or parsing.\n=== END CONSENSUS FINDINGS ===\n\n=== CONSENSUS TASK GAPS ===\n(No task gaps reported.)\n=== END CONSENSUS TASK GAPS ===\n\n=== FINDINGS FROM deepseek_deepseek-v4-pro ===\n- File: tests/test_review_autofix_review_pipeline_contract.py\n  Line or code reference: main() at line 2944; test defined at line 1762\n  Problem: The test function `test_review_collect_pr_metadata_helper_caps_fallback_graphql_batch_at_twenty_issues` is defined but never invoked from `main()`. Every other review-collect-pr-metadata test is called from `main()` (lines 2947-2953), but this one is absent.\n  Why it fails at runtime: The Python test runner in the file calls only functions explicitly listed in `main()`. Since this test is omitted, the 20-issue fallback cap behavior has zero test coverage. A regression that silently hydrates more than 20 issues would pass the suite undetected. The LAST RUN DIFF added two new tests to `main()` (lines 2950 and 2953) but did not fix this pre-existing gap, and did not introduce it \u2014 the caps test was defined in the ORIGINAL PR DIFF and was never wired into `main()` from the start.\n  ISSUE_CONFIDENCE: 5\n- File: scripts/review_collect_pr_metadata.sh\n  Line or code reference: `_fetch_linked_issue_bodies_graphql` lines 143-157 (jq transform `select(type == \"object\" and (.number? != null))`), called at lines 284-295\n  Problem: When the batched GraphQL fallback contains aliases for issue numbers that do not resolve (non-existent or inaccessible issues/PRs), the `jq` transform silently discards those null-resolved aliases via `select(type == \"object\" and (.number? != null))`. The caller at lines 286-295 only checks `_fallback_fetch_status` (transport/errors failure) and `_fallback_json != \"[]\"` (empty vs non-empty), but has no check comparing returned count against requested count. The old per-issue REST loop emitted a `::warning::` for each individual failed fetch, giving operators a per-issue signal.\n  Why it fails at runtime: An operator investigating why linked-issue context is incomplete would see no warning about the missing issues. The script fails open (continues with partial data), which is correct, but the loss of per-issue observability makes diagnosing gaps in linked-issue context materially harder. Pre-existing in the ORIGINAL PR DIFF, not introduced by LAST RUN DIFF.\n  ISSUE_CONFIDENCE: 3\n=== END FINDINGS FROM deepseek_deepseek-v4-pro ===\n\n=== FINDINGS FROM minimax_minimax-m2_5 ===\n- File: tests/test_review_autofix_review_pipeline_contract.py\n  Line or code reference: main() at line 2946, test defined at line 1762\n  Problem: The test function `test_review_collect_pr_metadata_helper_caps_fallback_graphql_batch_at_twenty_issues` is defined at line 1762 but is NOT called from `main()`. The LAST RUN DIFF added two other tests (`test_review_collect_pr_metadata_helper_fails_open_on_non_array_batch_input` and `test_review_collect_pr_metadata_helper_warns_when_fallback_graphql_returns_errors_without_data`) but omitted this 20-issue cap test.\n  Why it fails at runtime: The test runner invokes only functions listed in `main()`. Since this test is omitted, a regression in the 20-issue cap behavior would go undetected during CI runs.\n  ISSUE_CONFIDENCE: 5\n=== END FINDINGS FROM minimax_minimax-m2_5 ===\n\n=== FINDINGS FROM mistralai_mistral-small-2603 ===\n- File: scripts/review_collect_pr_metadata.sh\n  Line or code reference: lines 89, 92 (LAST RUN DIFF added guard)\n  Problem: The numeric validation path still uses shell arithmetic comparison `[ \"${count}\" -eq 0 ]` which could theoretically fail if `count` somehow becomes a non-integer despite the regex guard.\n  Why it fails at runtime: If `jq` returns a malformed or non-numeric value (e.g., empty string, null string), the arithmetic expression could produce \"integer expression expected\" errors even with the regex check in place, potentially causing the script to crash rather than fail gracefully.\n  ISSUE_CONFIDENCE: 4\n- File: scripts/review_collect_pr_metadata.sh\n  Line or code reference: lines 130-134 (LAST RUN DIFF added GraphQL error handling)\n  Problem: The GraphQL response error handling cannot detect cases where some requested issue aliases resolve to null/empty (non-existent issues), losing observability compared to the previous per-issue REST fallback.\n  Why it fails at runtime: When the GraphQL response contains fewer hydrated items than requested aliases, there is no warning logged, making missing fallback context harder to detect in production. The old REST loop explicitly warned for each failed individual fetch.\n  ISSUE_CONFIDENCE: 3\n- File: tests/test_review_autofix_review_pipeline_contract.py\n  Line or code reference: function `test_review_collect_pr_metadata_helper_caps_fallback_graphql_batch_at_twenty_issues` defined at line 1762, and main() invocation at line 2946\n  Problem: The new test function that verifies the 20-issue cap behavior is defined but not invoked from `main()`.\n  Why it fails at runtime: Since the test suite runs only the functions listed in `main()`, the cap behavior goes unverified and a regression would not be caught by CI. This defeats the purpose of adding the test.\n  ISSUE_CONFIDENCE: 5\n- File: scripts/review_collect_pr_metadata.sh\n  Line or code reference: usage site at lines 284-295\n  Problem: While the fail-open contract is preserved (warning plus empty context on GraphQL failure), the GraphQL error-path guards do not cover all possible failure modes from the primary `closingIssuesReferences` path.\n  Why it fails at runtime: A malformed or permission-denied GraphQL response on the primary query could produce an untested warning path or leak partial data, breaking the \"fail-open the same way\" contract subtly.\n=== END FINDINGS FROM mistralai_mistral-small-2603 ===\n\n=== FINDINGS FROM moonshotai_kimi-k2_5 ===\n- File: tests/test_review_autofix_review_pipeline_contract.py\n  Line or code reference: 1762 (test defined), 2944-2964 (main function)\n  Problem: The test function `test_review_collect_pr_metadata_helper_caps_fallback_graphql_batch_at_twenty_issues` is defined but never invoked from `main()`.\n  Why it fails at runtime: The test runner only calls functions explicitly listed in `main()`. Without invocation, the 20-issue cap behavior is not actually exercised in CI, creating a blind spot where regressions would go undetected.\n  ISSUE_CONFIDENCE: 5\n- File: scripts/review_collect_pr_metadata.sh\n  Line or code reference: 143-154 (jq transform in _fetch_linked_issue_bodies_graphql)\n  Problem: Non-existent or inaccessible issue numbers silently disappear from the GraphQL response. The jq transform filters with `select(type == \"object\" and (.number? != null))`, which drops null values without warning.\n  Why it fails at runtime: If a PR references an issue that doesn't exist or is inaccessible, the old per-issue REST loop would emit a `::warning::` per failure. The new batch GraphQL path provides no observability for partial failures, making debugging harder when linked issue context is unexpectedly missing.\n  ISSUE_CONFIDENCE: 4\n=== END FINDINGS FROM moonshotai_kimi-k2_5 ===\n\n=== FINDINGS FROM qwen_qwen3_6-plus ===\n- File: tests/test_review_autofix_review_pipeline_contract.py\n  Line or code reference: line 1762 (function definition) vs lines 2944-2964 (main() function)\n  Problem: The test `test_review_collect_pr_metadata_helper_caps_fallback_graphql_batch_at_twenty_issues()` is defined but never called from main(). This test validates that the 20-issue cap is enforced for the new batched GraphQL fallback.\n  Why it fails at runtime: The test runner only executes functions explicitly called from main(). This test has zero callers so a regression in the cap behavior would not be caught by the suite. The LAST RUN DIFF added two other new tests and called them from main(), but missed this one.\n  ISSUE_CONFIDENCE: 5\n=== END FINDINGS FROM qwen_qwen3_6-plus ===\n\n=== FINDINGS FROM x-ai_grok-4_20 ===\n- File: scripts/review_collect_pr_metadata.sh\n  Line or code reference: _fetch_linked_issue_bodies_graphql() lines 89-92 (count extraction + guard) and lines 130-134 (jq error check)\n  Problem: The guard `if ! [[ \"${count}\" =~ ^[0-9]+$ ]] || [ \"${count}\" -eq 0 ]` correctly catches the non-numeric case added by the previous autofix, and the new jq check handles HTTP-200 + errors-without-data. However, the transformed output always returns `number: (.number // 0)` even for PRs, which is inconsistent with GitHub's canonical numeric ID handling for cross-type references.\n  Why it fails at runtime: When a fallback resolves a PullRequest, downstream consumers expecting strictly positive integers may treat 0 as invalid, breaking linked-issue context rendering or JSON parsing in review prompts.\n  ISSUE_CONFIDENCE: 3\n- File: scripts/review_collect_pr_metadata.sh\n  Line or code reference: lines 281-295 (caller) and helper lines 127-134 + 158-162 (error detection + jq transform)\n  Problem: The new `_fetch_linked_issue_bodies_graphql` returns non-zero + `[]` for both transport failure and the specific GraphQL error shape, but the caller only checks `_fallback_fetch_status != 0` before deciding to emit the batched success message. A case where GraphQL returns partial results mixed with errors (possible on complex repository ACLs) is not warned.\n  Why it fails at runtime: Linked-issue context becomes silently incomplete (no fallback warning) while the old per-REST path emitted one warning per failing fetch; this weakens observability for the exact fallback path the issue t",
  "fingerprint": "a5edd115ff3e2972acef5c126651ec7d98a652890594e072b00ed94ab2fedfe3",
  "lineage": {
    "issue_number": null,
    "parent_ids": [],
    "pr_number": 3333,
    "run_id": "27469951911",
    "superseded_by": null,
    "supersedes": null
  },
  "provenance": {
    "actor": "shubhodeep1",
    "run_attempt": 1,
    "run_id": "27469951911",
    "source_refs": [
      "reviewer_consensus.txt"
    ],
    "workflow": "review_autofix"
  },
  "record_id": "mem_20260613152540_2c129853ad",
  "schema_version": "memory_record.v1",
  "scope": {
    "issue_number": null,
    "level": "run",
    "pr_number": 3333,
    "run_id": "27469951911"
  },
  "sensitive": false,
  "status": "candidate",
  "summary": "Reviewer consensus flagged 3 multi-reviewer item(s) across 6 total consensus item(s)",
  "timestamps": {
    "created_at": "2026-06-13T15:25:40Z",
    "promoted_at": null,
    "superseded_at": null
  }
}
