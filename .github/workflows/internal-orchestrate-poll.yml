name: "Internal: AI Orchestrate Poller"

on:
  schedule:
    - cron: '*/10 * * * *'

permissions:
  contents: write
  issues: write
  pull-requests: write
  actions: write

jobs:
  poll:
    uses: ./.github/workflows/orchestrate_poll.yml
    secrets: inherit
