name: "Internal: AI Implement"

on:
  issue_comment:
    types: [created]

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  implement:
    uses: ./.github/workflows/implement.yml
    secrets: inherit
