name: "Internal: AI Clarify"

on:
  issues:
    types: [opened]
  issue_comment:
    types: [created]

permissions:
  contents: read
  issues: write

jobs:
  clarify:
    if: ${{ !contains(github.event.issue.labels.*.name, 'ai:security-audit') }}
    uses: shubhodeep1/coding-workflows/.github/workflows/clarify.yml@main
    secrets: inherit
