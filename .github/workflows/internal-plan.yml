name: "Internal: AI Plan"

on:
  issue_comment:
    types: [created]

permissions:
  contents: read
  issues: write

jobs:
  plan:
    if: >-
      github.event.issue.pull_request == null &&
      startsWith(github.event.comment.body, '/answer')
    uses: ./.github/workflows/plan.yml
    secrets: inherit
