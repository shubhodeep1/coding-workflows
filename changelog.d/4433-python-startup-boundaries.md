<!-- changelog: fixed -->
- **Review and poller post-agent failures no longer leave a Python startup payload at the active checkout path.** Reconciliation failures quarantine the identity-checked worktree, validator units cannot read Git metadata, workflow-reference checks use staged code, and Python probes and summaries avoid importing from PR-controlled directories. Git credentials are stored outside the checkout and hidden from sandbox units instead of being exported to every job step. Cross-language changes keep uncertain access-control findings blocking.
