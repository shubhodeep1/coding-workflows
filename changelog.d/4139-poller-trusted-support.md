<!-- changelog: fixed -->

- The project security pass and lesson writers now execute SHA-verified support outside the integration checkout instead of running project-controlled scripts with poller credentials.
