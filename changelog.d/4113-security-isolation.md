<!-- changelog: security -->
- **Isolate workflow model writers and immutable support.** Implementation and validation agents now run under dedicated least-privilege identities, model-created files cannot bypass repair allow-lists through retained ownership, and issue phases fail closed when integration-ref resolution cannot prove the intended branch. Workflow support bundles, including orchestrate and orchestrate-poll, fail closed on missing or checkout-shadowed helper, instruction, and meta-prompt dependencies, while checkout documentation is treated as untrusted context.
