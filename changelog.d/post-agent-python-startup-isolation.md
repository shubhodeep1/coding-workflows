<!-- changelog: fixed -->
- **Post-agent validation now rejects ignored Python startup payloads before privileged workflow steps.** Implement, review editor, conflict resolver, review-blocked, and poller writer paths compare complete pre/post worktree manifests, quarantine hidden, ignored, package-form, and sourceless startup payloads, terminate on any guard failure before parsing or publication, and run output normalization and Python validation with isolated no-site startup in a credentialless environment outside the worktree.
