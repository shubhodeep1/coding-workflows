<!-- changelog: fixed -->
- **Review editor changes now reach the reviewed worktree.** Isolated review snapshots and transfers target the selected review worktree instead of the separate runner checkout; a missing or changed worktree fails closed before publication.
