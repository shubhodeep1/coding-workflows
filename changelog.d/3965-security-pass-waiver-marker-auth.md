<!-- changelog: security -->
- **Security-pass waivers and comprehensive promotion markers now fail closed on ambiguous or forged evidence.** Waivers require exact code-context fingerprints plus current maintain/admin authorization, while promotion markers are keyring-signed, bot-ID- and tracking-issue-bound, tied to the authenticated orchestrate dispatcher, and checked against the live smoke run's coverage-affecting inputs before promotion. Invalid or legacy marker cycles are retired automatically for a clean scheduled retry rather than waiting on manual label removal; transient marker lookup failures retain the cycle, defer callbacks and pre-dispatch verifying merges, and preserve already-dispatched promotion state.
