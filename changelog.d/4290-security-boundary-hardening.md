<!-- changelog: security -->
- **Harden AI workflow trust boundaries against prompt laundering, unauthorized provider spend, resolver tampering, and stale security waivers.** Implement helpers are now immutable and digest-bound across source and consumer repositories; post-failure diagnosis runs without GitHub authorization; the provider proxy enforces model, request, token, concurrency, and Decimal spend limits; integration resolution can edit only original textual conflict spans; and one module-aware causal validator governs waiver reuse in both the audit engine and poller.
