<!-- changelog: security -->
- **Keep project security passes and review recovery on verified trust boundaries.** Branch audits now run pinned, independently verified support actions and scripts outside the audited checkout. Orchestrator lesson events use the verified external support copy rather than integration-branch Python. A Claude-fixer verdict requests a fresh full same-head review and settled checks instead of authorizing auto-merge; incomplete reviews retry on the scheduled sweep. Review-blocked labels reach an issue only through verified bot-owned PR branch lineage, never PR prose or closing references.
