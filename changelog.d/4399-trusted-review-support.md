<!-- changelog: fixed -->
- **Review/autofix no longer runs PR-head support scripts with workflow credentials.** The gate resolves and verifies the reusable workflow's repository, permitted ref, and immutable commit, then every review job pins executable helpers and runtime assets to that SHA while keeping the PR checkout for review and edits. Missing required trusted support fails closed; optional features skip without executing PR worktree fallbacks.
