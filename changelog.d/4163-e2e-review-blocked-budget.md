<!-- changelog: fixed -->
- **The release E2E gate now reserves enough time to exercise review-blocked recovery and attributes the result to its own poller run.** Unsafe timeout combinations fail before test issues are created, while Phase 6 registers the branch-scoped run created after its dispatch so concurrent pollers cannot replace its status or logs. Transient pinned-run and issue-label reads retry within their existing bounded window instead of skipping the simulation.
