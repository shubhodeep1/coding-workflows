<!-- changelog: fixed -->
- **The release E2E gate now reserves enough time to exercise review-blocked recovery and attributes the result to its own poller run.** Unsafe timeout combinations fail before test issues are created, while Phase 6 consumes the run ID returned by its workflow dispatch so concurrent pollers cannot replace its status or logs. Transient pinned-run reads retry within their existing bounded window instead of skipping the simulation.
