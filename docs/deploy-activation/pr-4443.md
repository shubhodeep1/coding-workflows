# Deploy-Activation Log — Claude issue implementer (standalone issues → "Claude issue dispatcher" routine)

- Reference: PR #4443 (https://github.com/shubhodeep1/coding-workflows/pull/4443), branch `claude/gifted-einstein-nnfrmt`
- Deploy target: shubhodeep1/coding-workflows (+ the 13 consumers in `.github/ai/consumer_repos.json` via `@stable`)
- How it runs: `repository_dispatch` (`claude-issue`) sent by `clarify.yml` in every repo → `.github/workflows/claude-issue-intake.yml` (coding-workflows) → POST to the routine's `/fire` endpoint. coding-workflows calls `clarify.yml@main` (`internal-clarify.yml:18`); consumers call `clarify.yml@stable` (`workflow-templates/ai-clarify.yml:18`).
- Status: BLOCKED
- Last updated: 2026-09-26
- Last note: Step 8a done (routine model set to Opus 5.5). Step 8b (pause the promote cycle with `PROMOTE_CYCLE_ENABLED=false`) is current. Step 8 stays BLOCKED on the routine-path fix (session_01Pa5BhbdDuL6yhKLaLUYQyr).

## Runbook
1. [x] Prereqs: Homebrew, git, gh, jq, `gh auth login` (repo, workflow), clone; baseline read of `CLAUDE_ISSUE_*` vars/secrets   — done 2026-09-26: Homebrew installed; git 2.55.0, gh 2.101.0, jq 1.8.2; `gh api user` = shubhodeep1; cloned to ~/src/coding-workflows; no `CLAUDE_ISSUE_*` / `AI_ISSUE_IMPLEMENTER` vars set; secrets: `GH_PAT` present (updated 2026-05-17), `CLAUDE_ISSUE_ROUTINE_TOKEN` absent
2. [x] WEB UI: create routine "Claude issue dispatcher" (repo shubhodeep1/coding-workflows, Opus, same cloud env, minimal connectors, fixed prompt, no schedule)   — done 2026-09-26: operator confirmed; name `Claude issue dispatcher`, Opus 5.5, repo shubhodeep1/coding-workflows, environment `github`, Gmail/Google Drive/Claude Docs connectors removed, no schedule. The form required a trigger, so the API trigger was selected at creation.
3. [x] WEB UI: add API trigger, copy `trig_…` id, generate token (token never leaves the modal except into step 5)   — done 2026-09-26: routine id `trig_01GykbNLA7znHCJGWRetq4Ye` (format checked against `^trig_[A-Za-z0-9]+$`); token generated and stored by the operator, not shared
4. [x] Set repo var `CLAUDE_ISSUE_ROUTINE_ID` (read at `claude-issue-intake.yml` job env; intake requires `^trig_[A-Za-z0-9]+$`, `scripts/claude_issue_intake.sh` step 2)   — done 2026-09-26: `✓ Created variable CLAUDE_ISSUE_ROUTINE_ID`; `gh variable get` returns `trig_01GykbNLA7znHCJGWRetq4Ye`
5. [x] Set secret `CLAUDE_ISSUE_ROUTINE_TOKEN` via interactive prompt; verify with `gh secret list` (name only)   — done 2026-09-26: set via hidden `gh secret set` prompt (`✓ Set Actions secret CLAUDE_ISSUE_ROUTINE_TOKEN`); `gh secret list` shows `CLAUDE_ISSUE_ROUTINE_TOKEN 2026-09-26T04:31:24Z` and `GH_PAT 2026-05-17T02:25:27Z`
6. [x] Decide which consumers stay on Codex (Q/A, multi-select); `gh variable set AI_ISSUE_IMPLEMENTER --body codex -R <repo>` for each (read at `clarify.yml:418`, default `claude`)   — done 2026-09-26: operator decision Q1: B (direct switch at promotion, no canary), Q2: N (no consumer stays on Codex; an initial `Q2: all` was corrected before any variable was set). Read-only check: `AI_ISSUE_IMPLEMENTER` is `(not set)` on all 13 consumers, so all default to `claude` once `@stable` carries #4443
7. [x] Merge PR #4443 to main (after CI green + mergeable confirmed)   — done 2026-09-26 (out of order, before steps 2–6): merged by shubhodeep1 at 03:37 UTC as `666b9bb`; head `6b9f27c`
8. [ ] (BLOCKED 2026-09-26: plumbing passed, lifecycle failed; see Notes) Smoke test in coding-workflows (intake run green with `CLAUDE_ISSUE_INTAKE fired … session=`; `<!-- ai:claude-issue-dispatched:v1 -->` comment; then `<!-- ai:claude-issue-progress:v1 -->` + phase-1 PR); record create_session vs in-session fallback
8a. [x] WEB UI: set the routine's model to Opus 5.5 (the first smoke-test run served `claude-sonnet-5`)   — done 2026-09-26: operator confirmed; the selector's prior value was not reported, so the step 8 re-run's `last_served_model` confirms whether the setting applies
8b. [ ] Hold promotion: `gh variable set PROMOTE_CYCLE_ENABLED --body false -R shubhodeep1/coding-workflows` (read at `promote-main-to-stable.yml:138`, default `true`; operator Q6: A)
8c. [ ] Wait for the routine-path fix PR from session_01Pa5BhbdDuL6yhKLaLUYQyr to merge, then re-run the step 8 smoke test (full issue-mode chain, `claude/implement-plan-issue-<N>-…-phase-1` PR, Opus)
9a. [ ] Re-enable promotion: `gh variable set PROMOTE_CYCLE_ENABLED --body true -R shubhodeep1/coding-workflows` (after 8c passes)
9. [ ] Promote main → `@stable` (`promote-main-to-stable.yml`; its daily 00:00 UTC cycle may do this on its own) and confirm the `coding-workflows-stable-released` dispatch + consumer `update_workflows` sync
10. [ ] Verify LIVE in one Claude-routed consumer (test issue → `ai:claude`, dispatched comment, intake run fired)

## Notes
- 2026-09-25: first `/deploy-activate` pass stopped at the completeness gate: PR head `810d88b` had merge conflicts with main (two different CLAUDE.md §28 sections) and 2 failing tests in `tests/test_implement_issue_claude_command.py`.
- 2026-09-26: re-checked. Head `6b9f27c` ("Merge main into claude/gifted-einstein-nnfrmt") resolves the conflict by keeping main's §28 and adding issue mode to it; GitHub reports `mergeable=true`. `tests/test_implement_issue_claude_command.py`, `tests/test_claude_issue_route.py`, `tests/test_implement_plan_claude_command.py`: 85 passed locally on `6b9f27c`. CI on `6b9f27c` was still in progress; re-check at step 7.
- Not an `/implement-plan-claude` project: no `docs/implement-plan/` progress log, so no auto-decisions to review.
- Repo variables/secrets cannot be read from the web session (agent proxy refuses Actions variable/secret paths); step 1 reads them on the operator's Mac.
- Promotion is automated: `promote-main-to-stable.yml` runs a daily cycle (cron `0 0 * * *`) that gates `main`, runs proving/verifying apply-analysis runs, then promotes. After #4443 merges, consumers can switch to Claude without a manual step 9, so steps 2–6 must be done before the merge.
- Consumer handoff sends `repository_dispatch` to coding-workflows with the consumer's own `GH_PAT` (`scripts/claude_issue_handoff.sh`), so each consumer's `GH_PAT` needs `repo` scope on coding-workflows. Step 10 checks this.
- Routine token: never in chat, commands, this log, or commits. If pasted by mistake, regenerate it in the routine's API-trigger modal and redo step 5.
- Log PRs: #4509 (merged 2026-09-26 right after opening). Log persistence (operator Q1: B, 2026-09-26): push after every step, each update on a new branch `claude/funny-feynman-hzqvvr-<N>` (N = 2, 3, …) cut from main with its own docs PR (the 2026-09-26 resume session uses `claude/sharp-franklin-1hrznc-<N>` the same way); the merged branch cannot be reused (CLAUDE.md §21 guard, force-push blocked by repo rules).
- 2026-09-26 (resume, 04:06 UTC): #4443 is already on `main` (`666b9bb`), so coding-workflows routes its own standalone issues to Claude now, with no routine configured. Until steps 4–5 are done, any such issue here gets `ai:claude-handoff-failed` plus a retry/switch comment (visible and recoverable with `/reclarify`, not silent). At this check there were no `claude-issue-intake.yml` runs and no issues labelled `ai:claude` or `ai:claude-handoff-failed`.
- `@stable` is `fa3c585` (v1.29.11, 2026-09-24) and does not contain `666b9bb`, so consumers are still on Codex. Step 6 (Codex opt-outs) now has to be done before the next successful promotion instead of before the merge.
- `promote-main-to-stable.yml` has failed its last 5 daily runs (2026-09-22 to 2026-09-26). Latest: run 36205357822, `PROMOTE_CYCLE_FAILED reason=smoke_gate_failed run=36205375333` (the `test-and-mark-stable.yml` gate_only smoke gate). Promotion will not happen until that gate passes. Step 9 diagnoses it.
- claude.ai Routines docs (code.claude.com/docs/en/routines): API-only routines (no schedule) are supported; the API-trigger token is shown once and can be regenerated or revoked from the same modal; the `/fire` endpoint, `anthropic-beta: experimental-cc-routine-2026-04-01` and `claude_code_session_id` / `claude_code_session_url` response match `scripts/claude_issue_intake.sh`. The docs do not say whether routine runs get `create_session`; step 8 records which path the dispatcher took.
- 2026-09-26 step 6: `AI_ISSUE_IMPLEMENTER` is read only at `clarify.yml:418` (the other mentions are comments and docs). A failed consumer handoff does not fall back to Codex: `scripts/claude_issue_handoff.sh` labels the issue `ai:claude-handoff-failed`, comments, and exits 0, so the issue waits for `/reclarify` or `ai:codex`. With no canary, a broken consumer path would stall issues in all 13 consumers at once, which step 10 must catch quickly after promotion.
- 2026-09-26 step 8 smoke test, issue #4525 (README token-rotation docs):
  - **Passed (plumbing):**
    - `Internal: AI Clarify` routed it to Claude, labelled `ai:claude`, and posted the `ai:claude-issue-routed:v1` comment (04:40:08). The comment shows reason `repo_var` although the var is unset, because `clarify.yml:418` passes `vars.AI_ISSUE_IMPLEMENTER || 'claude'`, so `claude_issue_route.py` never sees `""`. This is cosmetic.
    - Intake run 36218509865 was green with `CLAUDE_ISSUE_INTAKE fired repo=shubhodeep1/coding-workflows issue=4525 trigger=opened http=200 session=cse_015RqYR4nvFgjzaLLtyshGnb`.
    - The `ai:claude-issue-dispatched:v1` comment carried the dispatcher session link (04:40:21).
  - **Dispatcher path: in-session fallback, NOT `create_session`.** Routine-fired sessions (`origin: fire_routine`, dispatcher `session_015RqYR4nvFgjzaLLtyshGnb`) do not get the claude-code-remote tools (`create_session`, `send_later`, `get_session`). They are not a user connector either (the account's connectors are Gmail, Google Calendar and Google Drive), so the routine's connector choice did not cause this.
  - **Model:** the dispatcher session's `last_served_model` was `claude-sonnet-5`, not Opus.
  - **Lifecycle not followed.** The fallback session recorded AD-1 ("direct minimal edit" instead of the `/implement-plan-claude` issue-mode chain, because the chain needs `create_session` / `send_later`) and AD-2 (a session-local `CronCreate` check-in). The result:
    - no `docs/plans/issue-4525-*` plan;
    - no `claude/implement-plan-issue-4525-…-phase-1` branch; it used branch `claude/vibrant-newton-1225p2`;
    - no conformance, security or validation pass.
  - **The PR merged before CI finished, as designed.** PR #4526 (README only, +9) was opened at 04:45:35 and merged at 04:45:56 by `Internal: AI Review & Autofix` run 36218784261, job `review / deterministic-skip-merge` (the existing docs-only fast path). It was not merged by the routine session. #4525 closed as completed.
  - **Implication.** `claude-issue-dispatch.md` step 3 says later stages "run in their own session started by its checker", but a routine run cannot start checker sessions. So #4443's "same lifecycle as a project" does not hold for routine-dispatched issues. coding-workflows is live on this path now; the 13 consumers join it at the next promotion.
- 2026-09-26 operator decisions after step 8:
  - Q1: #4526 was merged by the review-autofix deterministic-skip path (confirmed from run 36218784261).
  - Q2: A, hold `@stable` promotion until the routine-path fix is merged and a new smoke test passes.
  - Q3: B, coding-workflows stays on Claude meanwhile.
  - Q4: A, fix investigated in a new Opus session, `session_01Pa5BhbdDuL6yhKLaLUYQyr` (`/investigate-issue` on #4525).
  - Q5: A, set the routine's model to Opus 5.5 in the web UI.
- The promotion hold needs an enforcement mechanism. `promote-main-to-stable.yml` runs daily (cron `0 0 * * *`) and only its failing smoke gate is holding it today. The kill switch is repo var `PROMOTE_CYCLE_ENABLED` (read at `promote-main-to-stable.yml:138`, default `true`; `false` logs `PROMOTE_CYCLE_SKIPPED reason=disabled`), and it pauses every promotion, not only #4443's.
- 2026-09-26 operator Q6: A. Enforce the promotion hold with `PROMOTE_CYCLE_ENABLED=false` (step 8b) and re-enable it after the fixed smoke test passes (step 9a). Manual `workflow_dispatch` promotion is not affected by the kill switch, so none should be run while the hold is in place.
