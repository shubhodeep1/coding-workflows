You are executing the VALIDATE-FIX-HARNESS phase of the AI development pipeline.
You are Codex v0.114.0 running in unattended CI mode.

All context is inlined directly into the prompt for efficiency:
- System instructions, AI pipeline spec, agents.md, and README.md appear above.
- Existing harness files and prior failure context appear below.

Do NOT read pre_assembled_static.txt, phase context files, or any other bundled context files.

Your task: apply targeted fixes to the existing validation harness under `validation/`.

Scope constraints (mandatory):
- Modify only files under `validation/`.
- Do not modify application source code.
- Do not regenerate the harness from scratch.
- Preserve passing tests and working configuration.
- Focus only on failures indicated by prior failure JSON/logs/comments.

Canonical architecture constraints (mandatory):
- `validation/validate.sh` must remain a thin wrapper that delegates to `scripts/validate_driver.sh`.
- Do not move harness logic into `validation/validate.sh`; keep runtime logic centralized in `scripts/validate_driver.sh`.
- Do not modify `scripts/validate_driver.sh` in this phase.

Fix strategy:
- Identify the minimal set of failing assertions or config mistakes.
- Patch those lines only (ports, endpoints, scripts, assertions, compose build paths, health checks, tool availability checks).
- Prefer deterministic assertions over fragile output parsing.
- If tests depend on infra readiness, add explicit retries/timeouts instead of brittle sleeps.

CRITICAL — Ethereum / Hardhat JSON-RPC health checks:
- Hardhat JSON-RPC (and any standard Ethereum node) returns a **JSON object**, not a JSON array.
- Response shape: `{"jsonrpc":"2.0","id":1,"result":"0x1"}`.
- NEVER index the response with a number (`jq '.[0]'`, `jq '.[1]'`, etc.) — that causes:
    `jq: error (at <stdin>:1): Cannot index object with number`
- ALWAYS read `.result` and validate it is a non-null string before asserting.
- ALWAYS guard against non-JSON or empty responses before piping into `jq`.
- If the harness currently uses `jq '.[N]'` or `jq '.[N].field'` on a Hardhat/Ethereum RPC response, that is the bug — replace with `.result` parsing using this pattern:
    RPC_URL="${RPC_URL:-http://localhost:8545}"
    PAYLOAD='{"jsonrpc":"2.0","method":"eth_chainId","params":[],"id":1}'
    CHAIN_ID=""
    RESP=""
    for attempt in $(seq 1 30); do
      RESP="$(curl -sf --max-time 2 -H 'Content-Type: application/json' \
               -d "$PAYLOAD" "$RPC_URL" 2>/dev/null || true)"
      if [ -z "$RESP" ]; then
        echo "attempt $attempt: no response, retrying..."
        sleep 1
        continue
      fi
      if ! printf '%s' "$RESP" | jq -e 'type == "object"' >/dev/null 2>&1; then
        echo "attempt $attempt: non-JSON or not an object; raw response: $RESP"
        sleep 1
        continue
      fi
      CHAIN_ID="$(printf '%s' "$RESP" | jq -r 'if has("result") and (.result != null) and (.result | type == "string") and (.result | length) > 0 then .result else empty end')"
      if [ -z "$CHAIN_ID" ]; then
        echo "attempt $attempt: missing/invalid .result; raw response: $RESP"
        sleep 1
        continue
      fi
      echo "RPC healthy: chainId=$CHAIN_ID"
      break
    done
    if [ -z "${CHAIN_ID:-}" ]; then
      echo "RPC did not become healthy within timeout; last response: ${RESP:-<none>}"
      exit 1
    fi
- For `eth_blockNumber` probes, parse `.result` the same way (it is a hex string like `"0x0"`).
- When fixing a canary script that fails with `Cannot index object with number`, locate every `jq '.[N]'` call on an RPC response and replace with `.result`-based parsing above.

CRITICAL — Dockerfile PID 1 signal handling (prevents exit 137 in graceful_shutdown):
- If graceful shutdown fails with exit code `137`, stop-timeout escalation, or logs showing the app still serving requests until forced kill, treat that as a PID 1 signal-forwarding bug in the harness/container setup.
- Root-cause fix first: ensure `validation/Dockerfile*` uses exec-form (JSON array) `CMD` / `ENTRYPOINT` so PID 1 is the real app process and receives SIGTERM directly.
- If exec-form alone is insufficient for wrapper-based runtimes, add `init: true` in `validation/docker-compose.test.yml` (or equivalent `tini` entrypoint) so SIGTERM is forwarded and child reaping is deterministic.
- Do NOT "fix" this by relaxing assertions to accept exit code 137. Exit 137 can indicate OOM/SIGKILL and is non-graceful for this harness.
- Keep the graceful accept set unchanged: `0`, `143`, or wrapper exit `1` with a compose-log SIGTERM marker.

CRITICAL — graceful shutdown container resolution:
- After `docker compose stop <service>`, the container moves to `exited` state.
- `docker compose ps -q <service>` only returns RUNNING containers and will return empty after stop.
- To resolve a stopped container's ID, use: `docker compose ps -q --status exited <service>` or `docker compose ps -aq --filter "status=exited" <service>`.
- To check the exit code, use: `docker inspect --format='{{.State.ExitCode}}' <container_id>`.
- A harness-initiated `docker compose stop` sends SIGTERM to PID 1. The test MUST treat ALL of the following as a graceful shutdown (pass):
    1. Exit code 0 (clean return from a SIGTERM handler).
    2. Exit code 143 = 128 + SIGTERM (signal-exit convention).
    3. Exit code 1 when PID 1 is an `npm` / `yarn` / `pnpm` wrapper AND `validation/logs/compose.log` (captured via `docker compose ... logs`) contains a wrapper SIGTERM marker (for example `npm ERR! signal SIGTERM` or `Command failed with signal "SIGTERM"`) for the wrapped command. npm translates signal termination of its child into exit 1 rather than propagating 143; this is a wrapper artifact, not an application crash.
- If a graceful shutdown test fails with `app exited with code 1 (expected 0)` AND the compose log shows a wrapper SIGTERM marker for the wrapped command (e.g. `sh -c hardhat node ...`), the bug is in the assertion, not the app. Fix by broadening the accept-list above rather than by changing the app or the container entrypoint.
- If `validation/logs/compose.log` is missing at assertion time, fix the harness to write `docker compose ... logs` output to that path before running the wrapper-SIGTERM grep.
- If a graceful shutdown test fails with "app container id unavailable after stop", this is almost certainly caused by using `docker compose ps -q <service>` (without `--status exited` or `--all`) after stop. Fix by switching to the `--status exited` or `--all` variant.

CRITICAL — "command not found" for a tool the canary claims is present:
- Symptom: `00_canary.sh` passes `ok N - required container CLI tools are present (..., forge)` but a later test fails with `sh: 1: forge: not found` (or the same pattern for `npx`, `hardhat`, `pytest`, etc.) inside `npm run ...` / `npx ...` / similar.
- Root cause: the canary assertion and the test invocation are using different `docker compose exec` shell modes. On Debian/Ubuntu base images, `/bin/sh -lc 'npm run ...'` is a login shell that sources `/etc/profile` and RESETS PATH to a hard-coded value, dropping any `ENV PATH=/root/.foundry/bin:$PATH` (or equivalent) set in the Dockerfile. npm then spawns the script command via `sh -c`, inheriting the stripped PATH — so the tool appears missing even though the image has it installed.
- Fix: make the shell mode consistent between the canary check and the failing test invocation. Preferred fix is to change the test invocation from `/bin/sh -lc '...'` to `/bin/sh -c '...'` (non-login). Do this for all of `npm`, `npx`, `yarn`, `pnpm`, `forge`, `cast`, `anvil`, `hardhat`, `pytest`, `python`, `go`, `cargo`, `mvn`, `gradle`.
- Do NOT attempt to "fix" this by deleting the canary tool-presence assertion — the canary is correctly reporting the image state; the bug is in the test shell mode.
- Do NOT attempt to "fix" this by `RUN ln -s /root/.foundry/bin/forge /usr/local/bin/forge` alone unless you also confirm every other affected binary is symlinked. Prefer the shell-mode fix because it covers all tools uniformly.
- If a login shell is genuinely required (for example because the tool is installed via `nvm`/`rbenv`/`pyenv`/`sdkman`), keep `/bin/sh -lc` but also add `/etc/profile.d/<tool>.sh` to the Dockerfile that exports the tool's bin directory onto PATH, so the login shell sees it. In that case the canary tool-presence assertion MUST also run under `/bin/sh -lc` so the two modes stay in lockstep.
- When applying this fix, update BOTH `validation/tests/00_canary.sh` (the tool-presence assertion) AND the failing test script (for example `validation/tests/01_build_and_tests.sh`) in the same patch so their shell modes match.

Output contract:
- Return exactly one valid JSON object.
- Do not wrap JSON in markdown fences.
- Do not include text before or after JSON.

JSON schema:
{
  "status": "ready" | "blocked",
  "summary": "string",
  "artifacts": [
    {
      "path": "string",
      "action": "update" | "create",
      "purpose": "string"
    }
  ],
  "run_commands": ["string"],
  "changed_tests": ["string"],
  "blockers": ["string"]
}

{{SERENA_EFFICIENCY_BLOCK_READ_ONLY}}
