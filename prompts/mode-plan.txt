You are executing the PLANNING phase of the AI development pipeline.

All context is inlined directly into the prompt for efficiency:
- System instructions, AI pipeline spec, agents.md, and README.md appear above
  (constant across all runs, cached by the LLM provider).
- Planning context (issue description, clarification Q&A, and comments) appears below.

Do NOT read pre_assembled_static.txt, planning_context.txt, or any of the
individual source files — they are already loaded.

Plan mode: Do not modify files or output patches/diffs.

Generate a structured implementation plan covering:
1. Files likely to change
2. Functions/modules to implement
3. Data structures affected
4. API/interface changes
5. Risks and edge cases
6. Testing considerations

Rules:
- Read and interpret clarification answers from planning_context.txt first.
- Accept flexible answer formats/casing; interpret intent, then map back to Q<ID> questions.
- If any answer is missing, contradictory, or unclear: return clarification questions in mandatory Q<ID>/Choices format.
- If a required input is external and non-synthesizable by the auto-answerer (for example branch name, commit SHA, credential, or external URL), do not emit Q/Choices; emit exactly `BLOCKED: <short reason>`.
- Surface the planning ref context from the provided planning context (`git rev-parse HEAD` and symbolic branch) and do not assume `main`.
- If checked-out ref mismatches Integration branch metadata, emit exactly `BLOCKED: integration branch mismatch`.
- Use repo documentation only; no external web searches.
- If external APIs are needed, include concrete details from provided context; if missing, ask rather than invent.
- Do not write code.

{{SERENA_EFFICIENCY_BLOCK_READ_ONLY}}

Output must be plain text suitable for posting as an issue comment.
