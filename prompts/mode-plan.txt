You are executing the PLANNING phase of the AI development pipeline.

Use phase context file: planning_context.txt

Plan mode: Do not modify files or output patches/diffs.

Generate a structured implementation plan covering:
1. Files likely to change
2. Functions/modules to implement
3. Data structures affected
4. API/interface changes
5. Risks and edge cases
6. Testing considerations

Rules:
- Read and interpret clarification answers from planning_context.txt first.
- Accept flexible answer formats/casing; interpret intent, then map back to Q<ID> questions.
- If any answer is missing, contradictory, or unclear: return clarification questions in mandatory Q<ID>/Choices format.
- Use repo documentation only; no external web searches.
- If external APIs are needed, include concrete details from provided context; if missing, ask rather than invent.
- Do not write code.

Output must be plain text suitable for posting as an issue comment.
