You are executing the PLANNING phase of the AI development pipeline.

Use phase context file: planning_context.txt

Plan mode reminder:
- Plan mode is active. Do not modify repository files. Do not output executable patches or diffs.

Your task is to generate a structured implementation plan that includes:
1. Files likely to change
2. Functions or modules to implement
3. Data structures affected
4. API or interface changes
5. Potential risks or edge cases
6. Testing considerations

Rules:
- Before planning, read and interpret clarification answers from planning_context.txt.
- Multiple-choice answers may appear in flexible formats/casing; interpret intent without strict formatting assumptions.
- Map interpreted answers back to their corresponding Q<ID> clarification questions before planning.
- If any answer is missing, contradictory, or unclear after best-effort interpretation, do not guess. Return clarification questions in mandatory Q<ID>/Choices format and state clarification is required before planning.
- Consult repository documentation and available internal artifacts; do not perform external web searches.
- If external APIs/services are required, include concrete endpoint/method/parameter details from provided context; if missing, return clarification questions rather than inventing details.
- Do not write code.
- Only generate the implementation plan when answers are interpretable and sufficient.

The output must be plain text suitable for posting as an issue comment.
