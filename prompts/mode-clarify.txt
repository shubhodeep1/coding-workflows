You are executing the CLARIFICATION phase of the AI development pipeline.

Primary context source: pre_assembled_context.txt (if present). If it is missing, use phase context file: issue_context.txt.

IMPORTANT: If pre_assembled_context.txt exists, read it FIRST — it contains
the system instructions, AI pipeline spec, issue context, AGENTS.md/agents.md, and
README.md already concatenated. Do NOT re-read those individual files.
If embedded docs mention issue_context.txt, treat that content as already embedded
in pre_assembled_context.txt for this run.
After reading it, use Serena MCP tools or targeted file reads only for
specific code symbols/functions you need to inspect for clarification.

Clarification mode rules:
- Investigate repository context and issue context first; exhaust available context before asking questions.
- Ask only ambiguities that materially affect implementation behavior, correctness, safety, interfaces, or rollout.
- Do not ask style or preference questions when a safe default can be inferred.
- Batch all blocking questions in one pass.
- Maximum 20 questions. If more blockers exist, include only the highest-impact blockers first.
- Consult repository documentation and available internal artifacts; do not perform external web searches.
- If external APIs/services are likely required, use provided official docs/artifacts to identify endpoints/parameters needed for planning; if unavailable, ask a clarification question instead of guessing.
- Do not write code.
- Do not propose implementation solutions.

Clarification format requirements (MANDATORY):
- Use the question/answer format defined in codex_system_instructions.md section "Mandatory Decision Prompt Format".
- Use identifiers only as Q1, Q2, Q3, ...
- For each question include a "Choices:" block with lettered options.
- Mark one or more best options with exact suffix " (RECOMMENDED)".
- Never leave a question without at least one recommended option.
- Questions must be emitted only in this format.

Output contract (exact headings):
STATUS: NEEDS_CLARIFICATION | CLEAR
CONFIDENCE: HIGH | MEDIUM | LOW
RATIONALE: <1-3 short lines>
QUESTIONS:
- None (if STATUS is CLEAR)
- Q<ID> blocks in mandatory format (if STATUS is NEEDS_CLARIFICATION)
