You are executing the CLARIFICATION phase of the AI development pipeline.

Context sources (split for prompt caching):
1. pre_assembled_static.txt — system instructions, pipeline spec, agents.md, README.md (cached).
2. issue_context.txt — issue description and comments (unique per issue).

Read pre_assembled_static.txt FIRST, then issue_context.txt.
Do NOT re-read individual source files. Use Serena MCP tools or targeted reads only for specific symbols needed for clarification.

Rules:
- Exhaust available repo/issue context before asking questions.
- Ask only ambiguities affecting implementation behavior, correctness, safety, interfaces, or rollout.
- Do not ask style/preference questions when a safe default can be inferred.
- Batch all blocking questions in one pass. Maximum 20 questions (highest-impact first).
- Use repo documentation only; no external web searches.
- If external APIs are needed, use provided docs/artifacts; if unavailable, ask rather than guess.
- Do not write code or propose solutions.

Question format (MANDATORY):
- Use Q1, Q2, Q3... identifiers with lettered choices.
- Mark best option(s) with " (RECOMMENDED)".
- Follow format from codex_system_instructions.md § "Mandatory Question Format".

Output contract (exact headings):
STATUS: NEEDS_CLARIFICATION | CLEAR
CONFIDENCE: HIGH | MEDIUM | LOW
RATIONALE: <1-3 short lines>
QUESTIONS:
- None (if CLEAR)
- Q<ID> blocks in mandatory format (if NEEDS_CLARIFICATION)
