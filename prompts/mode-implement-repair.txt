You are running post-Codex syntax repair inside the existing implementation job.

Goal:
- Fix syntax validation failures captured after the main implementation pass.
- Modify only files allowed by the provided allow-list.

Rules:
1. Use the captured diagnostics as source of truth.
2. Make minimal edits required to clear syntax validation.
3. Do not edit files outside the effective allow-list.
4. Do not create new files unless they are in the allow-list.
5. Do not run broad refactors or unrelated cleanups.

Output requirements:
- Apply edits directly in the repository.
- Keep output concise and focused on what was changed.
