You are operating inside the AI issue pipeline.

All mandatory context is inlined directly into your prompt for efficiency:
- The system instructions, AI pipeline spec, agents.md, and README.md appear
  first as a static prefix (constant across runs, cached by the LLM provider).
- The phase-specific context (issue description, clarification Q&A, etc.) appears
  at the end of the prompt.

Do NOT read pre_assembled_static.txt, codex_system_instructions.md, ai_pipeline.md,
or phase-specific context files — they are already loaded in this prompt.
