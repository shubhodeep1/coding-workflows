You are executing the IMPLEMENTATION phase of the AI development pipeline.

All context is inlined directly into the prompt for efficiency:
- System instructions and AI pipeline spec appear above (constant across all runs,
  cached by the LLM provider).
- Implementation context (issue description, clarification answers, approved plan,
  and comments) appears below.

Do NOT read pre_assembled_static.txt, implementation_context.txt, or any of the
individual source files — they are already loaded.

Build mode reminder:
- Build mode is active. You may modify repository files and run required validations.

Implement the approved implementation plan. Modify only the files the plan requires; keep changes minimal and safe.

Restrictions:
- Do not ask clarification questions.
{{WORKFLOW_EDIT_RESTRICTION}}
- Do not modify ai_pipeline.md or codex_system_instructions.md.

Sibling-conflict discipline (prevents merge conflicts with parallel sibling
orchestrator issues that may be editing adjacent code):

- If the issue body lists `files_touched:` (machine-readable or prose form) or
  names specific files in its scope/acceptance criteria, STAY INSIDE that
  file set. Do not opportunistically edit, reformat, renumber, or refactor
  files outside the stated scope — other sibling issues in the same wave may
  be editing them right now.
- Prefer append-only edits in shared documentation (`README.md`, `agents.md`,
  CHANGELOG, operator docs): add new sections under existing stable HTML
  anchors of the form `<!-- anchor:NAME -->` rather than renumbering numbered
  lists, reflowing paragraphs, or rewriting surrounding content. If the
  target doc has no relevant anchor, add a new anchor comment alongside
  your insertion instead of restructuring unrelated sections.
- Do not delete, rename, or reorder existing anchors. Anchors are contract
  insertion points used across the orchestrator pipeline.
- When your change intersects an anchor, make the minimal edit required and
  leave untouched prose intact byte-for-byte.

Dependency / lockfile discipline (when touching dependency manifests):

If your patch edits a dependency manifest — adding, removing, bumping, or
changing the specifier of any dependency — you MUST update the corresponding
lockfile in the same patch so that `npm ci` / `uv sync --frozen` / `go mod
download` / `cargo build --locked` will succeed without drift. Lockfile drift
causes `npm ci` to exit with `EUSAGE` / `Missing: <pkg> from lock file`, `uv
sync --frozen` to fail resolution, `go mod verify` to report `sum mismatch`,
and `cargo build --locked` to fail with `the lock file ... needs to be
updated` — all of which break Docker builds and CI.

Manifest ↔ lockfile pairs to keep in sync:
- Node.js: `package.json` ↔ `package-lock.json` (npm), `yarn.lock` (yarn),
  `pnpm-lock.yaml` (pnpm). Regenerate via `npm install` (NOT `npm ci`),
  `yarn install`, or `pnpm install` in the project root.
- Python: `pyproject.toml` / `requirements*.in` ↔ `uv.lock` (uv),
  `poetry.lock` (poetry), `requirements*.txt` compiled via `pip-compile`.
  Regenerate via `uv lock`, `poetry lock --no-update`, or
  `pip-compile requirements.in` respectively.
- Go: `go.mod` ↔ `go.sum`. Regenerate via `go mod tidy` followed by
  `go mod download`.
- Rust: `Cargo.toml` ↔ `Cargo.lock`. Regenerate via `cargo update -p <pkg>`
  for a single bump or `cargo generate-lockfile` for a full resync.

Rules:
- Commit the regenerated lockfile alongside the manifest change in the same
  patch. Never split them across commits, and never leave a lockfile stale
  "to be regenerated later".
- If the environment lacks the tool required to regenerate the lockfile,
  STOP and record this as a blocker in your summary instead of committing a
  partial change.
- If your patch does NOT touch any dependency manifest, do not regenerate
  lockfiles as an unrelated side effect — that violates the minimal-change
  rule.

Data-file output discipline (when writing YAML or JSON):

- When writing files under `db/contracts/` or any other YAML/JSON path,
  emit raw YAML or raw JSON only. Never wrap the file body in markdown
  code fences (` ``` ` or ` ```yaml ` / ` ```json `), and never include
  language tags, fence markers, or surrounding prose in the file. The
  file's first byte must be valid YAML/JSON, and its last byte must be
  valid YAML/JSON — anything else is a syntax error that breaks the
  post-Codex validation step.

- YAML reserved-indicator quoting. When writing or editing
  `*.yml` / `*.yaml` files, any scalar (list item or mapping value) whose
  first non-whitespace character is one of `` ` ``, `@`, `*`, `&`, `|`,
  `>`, `!`, `%`, `#`, `?`, `,`, `[`, `]`, `{`, or `}` MUST be wrapped in
  double quotes. Plain (unquoted) scalars starting with these characters
  are reserved indicators in YAML 1.2 and cause
  `yaml.scanner.ScannerError: ... found character 'X' that cannot start
  any token` at parse time. This is enforced by
  `scripts/validate_changed_files_syntax.sh` and a single failure burns a
  post-Codex repair attempt. Common cases: prose-heavy invariants in
  `db/contracts/*.yml` mentioning backticked technical terms, list items
  beginning with `@username` mentions, or a `*` glob pattern.

If clarification context is incomplete, apply the safest minimal interpretation and explicitly record assumptions in your output/log summary without asking interactive questions.

{{SERENA_EFFICIENCY_BLOCK_READ_WRITE}}

Your output should be repository changes only.
