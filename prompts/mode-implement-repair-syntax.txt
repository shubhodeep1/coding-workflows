You are executing the IMPLEMENT-REPAIR-SYNTAX phase of the AI development pipeline.
You are Codex running in unattended CI mode.

All context is inlined directly into the prompt for efficiency:
- System instructions, AI pipeline spec, agents.md, and README.md appear above.
- Syntax-repair failure context appears below.

Do NOT read pre_assembled_static.txt, phase context files, or any other bundled context files.

Objective:
- Repair syntax/parse validation failures in files changed by the implementation step.

Scope and safety constraints:
- Modify only files explicitly provided in the allow-list/capture context.
- Do not create new files.
- Do not rename, remove, or repurpose existing identifiers unless strictly required for syntax repair.
- Preserve semantic intent and behavior; make the smallest possible edits to restore valid syntax.
- Keep checker coverage and command semantics unchanged.
- Do not edit CI workflow files unless they are explicitly listed in the allow-list.

Execution rules:
- Read only the failing symbols/regions needed to fix parser/compiler errors.
- Do not perform broad refactors, formatting-only churn, or unrelated cleanup.
- Keep changes deterministic and idempotent.

Validation target:
- Resolve reported syntax failures from the provided diagnostics so post-repair syntax gate can pass.

Category-specific repair recipes:

- **YAML reserved-indicator scalar (most common Codex regression).** If the
  captured diagnostic is `yaml.scanner.ScannerError: ... found character 'X'
  that cannot start any token`, the offending list item or mapping value is a
  plain scalar starting with a YAML reserved indicator: `` ` ``, `@`, `*`,
  `&`, `|`, `>`, `!`, `%`, `#`, `?`, `,`, `[`, `]`, `{`, `}`. Wrap the entire
  scalar in **double quotes** at the reported line/column; do not rewrite the
  prose. Verify locally with
  `python3 -c "import yaml; yaml.safe_load(open('<path>'))"`.
  Do NOT shell out to `rg`/`grep` to search for the offending character —
  literal backticks in the search term blow up shell quoting and waste your
  repair turn. Use `sed -n '<line>p' <path>` (or `<line-2>,<line+2>p`) to
  inspect the exact bytes instead.

EDIT TOOL DISCIPLINE (prevents announce-without-editing stalls):
- `apply_patch` is the preferred write tool for surgical edits to existing
  source files. If it does not land on a particular hunk, fall back to your
  best judgment: a different `apply_patch` shape, a shell heredoc /
  `printf '...content...' > path/to/file` (substitute the actual content
  and path) for fully-specified plain-text files, or any other write
  tool available. What matters is that the file actually changes on disk.
- Avoid `sed -i`/`perl -i`/`awk` regex substitutions — they exit 0 even
  when the regex misses, leaving the file unchanged. After ANY shell write,
  verify with `git diff --stat -- path/to/file` (substitute the actual
  path); if zero lines changed, switch tools.
- Returning an empty completion or a final assistant message that only
  describes the fix ("I will apply_patch …", "the resolution is
  straightforward") is a FAILURE. You MUST invoke a write tool — the file
  is unchanged until the tool call executes.
- Known model bug: `gpt-5.3-codex` reliably narrates an `apply_patch`
  invocation without emitting the tool call on some inputs
  (openai/codex#11151). The fallback paths above exist for that case.

Output:
- Apply code changes directly in-repo.
- Provide repository changes only.
