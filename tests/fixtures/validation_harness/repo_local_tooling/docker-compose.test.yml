services:
  app:
    init: true
    build:
      context: ..
      dockerfile: out/Dockerfile.app
    working_dir: /workspace
    env_file:
      - validate.env
    environment:
      APP_SERVICE: "${APP_SERVICE:-app}"
      APP_URL: "${APP_URL:-}"
